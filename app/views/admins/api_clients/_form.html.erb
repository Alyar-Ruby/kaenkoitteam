		<%= simple_form_for [@api_client], :html => { :class => 'form-horizontal' } do |f| %>
		<div class="well-formbox">  
			<% if @api_client.errors.any? %>
			<div id="errorExplanation">
				<h2><%= pluralize(@api_client.errors.count, "error") %> prohibited this user from being saved:</h2>
				<ul>
				<% @api_client.errors.full_messages.each do |msg| %>
				  <li><%= msg %></li>
				<% end %>
				</ul>
			</div>
			<% end %>
			<div class="control-group">
				<label class="control-label"> Email: </label>
				<div class="controls">
					<%= f.input :email,  :label=>false %>
				</div>
			</div>
			<div class="clearfix form-actions">
				<div class="col-md-offset-3 col-md-9">
					<%= f.submit :class => 'btn btn-info' %>
				 </div>
			</div>
			</div>
		<% end %>
	

