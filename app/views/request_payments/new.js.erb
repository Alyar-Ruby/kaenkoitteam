$("#ajax-container").html("<%= j(render 'form') %>")
$(".ajax-loading").hide()
$("#request_payment").removeClass("active").addClass("active")
$("#create_invoice").removeClass("active")
$("#manage_invoice").removeClass("active")
$(function() {
	history.pushState(null, "", "<%= new_request_payment_path %>")
	$(window).bind("popstate", function(){
    $.getScript(location.href)
  })
})